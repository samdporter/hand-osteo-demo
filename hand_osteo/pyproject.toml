[project]
name = "hand-osteo"
version = "0.1.0"
description = "HandOsteo MONAI Deploy application"
requires-python = "==3.11.*"
dependencies = [
    "monai-deploy-app-sdk==2.0.0",
]

[tool.uv]
package = false
