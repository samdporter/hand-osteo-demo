[project]
name = "hand-osteo-demo"
version = "0.1.0"
description = "Minimal HandOsteo MONAI Deploy application shape"
requires-python = "==3.11.*"
dependencies = [
    "monai-deploy-app-sdk==2.0.0",
]

[tool.uv]
package = false
